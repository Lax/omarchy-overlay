--- a/bin/omarchy-upload-log
+++ b/bin/omarchy-upload-log
@@ -127,9 +127,9 @@
   {
     echo ""
     echo "========================================="
-    echo "INSTALLED PACKAGES (pacman -Q)"
+    echo "INSTALLED PACKAGES (qlist -I)"
     echo "========================================="
-    pacman -Q 2>/dev/null || echo "Failed to get package list"
+    qlist -I 2>/dev/null || echo "Failed to get package list"
   } >>"$TEMP_LOG"
 
   if [[ ! -s $TEMP_LOG ]]; then
